<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:dubbo="http://code.alibabatech.com/schema/dubbo"
       xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd
       http://code.alibabatech.com/schema/dubbo http://code.alibabatech.com/schema/dubbo/dubbo.xsd">

    <!-- 提供方应用信息，用于计算依赖关系 -->
    <dubbo:application name="oms-camera-service"  />

    <!-- 注册中心服务地址 -->
    <dubbo:registry id="zookeeper" protocol="zookeeper" address="10.45.47.90:2121" />

    <!-- 用dubbo协议在30001 -->
    <dubbo:protocol name="dubbo" port="30001" />

    <!-- start 声明需要暴露的服务接口 -->
    <!--测试-->
    <dubbo:service interface="com.iwhalecloud.retail.camera.service.ICameraService" ref="cameraService"
                   version="1.0" registry="zookeeper"/>
    <!-- end 声明需要暴露的服务接口 -->

    <!-- start 具体服务接口的实现 -->
    <bean id="cameraService" class="com.iwhalecloud.retail.oms.service.impl.CameraServiceImpl" />
    <!-- end 服务接口的实现 -->



</beans>
